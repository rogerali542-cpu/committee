# 敏感信息保护 — 整改清单与方案

> 对照《敏感信息保护.md》四大原则，逐条核对本项目现状。
> 扫描日期：2026-07-11 ｜ 范围：backend（Spring Boot）+ application.yml + git 历史
> 一句话结论：**git 层面安全（密钥从未入库），但本地配置的密钥/密码管理、PII 加密与掩码尚未落实，与规范有差距。**

---

## 现状速览

**✅ 已安全**
- `application.yml` 已被 `backend/.gitignore` 忽略，**从未进入版本库**；TOS / 豆包密钥在 git 历史中零记录。
- 已有两处业务级脱敏（物业公示不含来访业主姓名房号；对外部角色隐藏管理信息）。
- `show-sql: false`（SQL 不打印到日志）。

**⚠️ 待整改**
- 数据库密码明文硬编码；多个真实密钥作为 `${ENV:默认值}` 的默认值明文落在 application.yml。
- 无任何密码/数据加密基础设施（无 BCrypt / AES / PasswordEncoder）。
- 手机号等 PII 明文存储、无掩码显示。
- 无敏感信息访问审计日志；`com.ywh: DEBUG` 生产可能打印敏感数据。

---

## P0 · 密钥保护 ✅ 已完成（2026-07-11，提交 `122ae21`）
> 对应规范：「加密密钥禁止硬编码」「密钥分环境变量 + key文件(600)拼接」

- [x] **数据库密码移出配置**：已改 `${DB_PASSWORD}`（无默认，缺失 fail-fast），真值移到 `backend/.env`。
- [x] **清空 application.yml 里所有真实密钥默认值**：jwt.secret / 豆包 asr 三项 / llm.api-key / ocr.internal-token / tos access-key+secret-key 全改 `${ENV}`，真值移到 `backend/.env`（gitignore）。
- [x] **核查部署配置** `deploy/docker/application-docker.yml`：已确认全用 `${ENV}` 占位、无硬编码密钥（平台直注环境变量，符合规范）。
- [x] **.gitignore 兜底复核**：`backend/.env`、`.env.local` 已忽略；`application.yml`、`application-local.yml` 本就忽略且历史干净。
- [x] **本地注入通道**：`dev-start.ps1` 启动后端前 `Get-Content backend\.env -Encoding UTF8` 注入；新增 `backend/.env.example` 模板入库。已用独立临时实例(8081)验证：去明文后配置仍正常启动、9 密钥注入生效、8080 主实例未受影响。
- [ ] **（进阶，未做）密钥拼接方案**：密钥分两半（环境变量 + `key 文件 chmod 600`）容器启动时拼接——按需再上。

## P1 · 数据加密与脱敏
> 对应规范：「密码 HASH+SALT」「API Key AES-256-GCM」「PII 加密存储 + 掩码显示 138****0000」「传输用 HTTPS」

- [ ] **确认登录方式**：项目未见 `User.password` 字段与密码加密——若为微信 openid 无密码登录，则「密码 HASH+SALT」**N/A**；若存在密码，必须改 BCrypt(含 salt)，禁止明文/可逆。
- [ ] **手机号 `User.phone` 存储加密**：引入 AES-256-GCM（密钥按 P0 方案管理），入库密文；或数据库列级加密。
- [ ] **PII 掩码显示**：新增统一脱敏工具（手机号 `138****0000`、姓名 `张*`、地址截断），前端展示 `realName / phone / Community.address` 时一律走掩码，仅授权场景可见明文。
- [ ] **生产强制 HTTPS**：前端录音已依赖 HTTPS；确认后端对外链路（含 `/asr-ws`）全程 TLS。
- [ ] **日志脱敏**：生产将 `logging.level.com.ywh` 从 `DEBUG` 降为 `INFO`；排查现有日志有无打印 phone / token / 密钥，统一脱敏后再记录。

## P2 · 访问审计与数据最小化
> 对应规范：「访问明文敏感信息需记审计日志，且日志脱敏」「只收集必要数据、优先设备端处理」

- [ ] **敏感信息访问审计**：对「查看明文手机号 / 导出委员名册」等操作记审计日志（谁、何时、访问了什么），日志内容本身脱敏。
- [ ] **数据最小化复查**：核对 `User / UserRoleEntity / LearningSignIn / Community` 收集的字段是否都为业务必需，去掉过度收集项。
- [ ] **设备端优先**：能在端上处理的（如录音本地转写预处理）尽量不上传原始敏感数据。

---

## 建议落地顺序
1. **P0 密钥**（半天内可完成，纯配置改造，收益最高）→ 改完需补齐环境变量并重启后端验证。
2. **P1 手机号加密 + 掩码**（涉及 entity/DTO/前端，需数据迁移方案：存量明文回填密文）。
3. **P2 审计 + 数据最小化**（随功能迭代持续推进）。

> ⚠️ P0/P1 会改动**正在运行的后端配置与数据结构**，动手前先确认环境变量清单齐全、并对存量数据做加密回填，避免重启后服务起不来或历史手机号读不出。
