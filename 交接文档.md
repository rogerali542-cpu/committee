# 业委会智能履职辅助系统 · 工作交接文档

> 交接人：__________　接手人：__________　交接日期：2026-08-04
>
> 这份文档的目标：让你在**半天内**能跑起来、看懂结构，在**一周内**能独立改需求。
> 先读第一、二章（知道这是什么、做到哪了），再照第四章把环境跑起来，
> 遇到具体业务问题回头查第五、六章。

---

## 一、这个软件是什么

### 1.1 一句话

给**居民区业主委员会**用的移动端履职工具：让平均年龄 60 岁以上的业委会成员，
用手机完成"开会—记录—表决—出纪要—归档"的全流程，并让街镇、居村两级能在线看到履职情况。

### 1.2 它解决的真实问题

这不是一个"把线下流程搬到线上"的常规 OA。真实痛点是：

| 痛点 | 系统的应对 |
|---|---|
| 开会靠微信群通知、纸质签到、手写记录 | 通知送达可追踪、扫码签到、现场录音 |
| 会后靠回忆整理纪要，滞后一周、要素残缺 | **AI 语音转写 + 大模型自动生成纪要**，10 分钟出初稿 |
| 签到人数、票数散落在纸上，无法追溯 | 表决过程结构化留痕，过半判定自动算 |
| 街镇无法掌握业委会运行情况 | 履职档案与统计看板 |
| 公示件是普通文档，无法自证未被篡改 | 纪要**哈希上链存证** + 一键验真 |
| 通用软件字号小、层级深，老人用不了 | 全系统按**适老化规范**设计（见 §5.4） |

### 1.3 用户是谁（这条决定了所有设计取舍）

**主要用户是 60 岁以上、智能手机不熟练的业委会成员。**
这不是修辞，是每一个界面决策的依据。举例：

- 所有可点区域 ≥ 54px —— 手指粗、点不准
- 一屏只有一个主按钮 —— 多个动作会让人不知道该点哪个
- 不用"提交/确认/保存"这类抽象词，用"通知委员""开始录音"这类具体动作
- 三色制（见 §5.4）—— 颜色多了老人分不清哪个重要

**接手后最重要的一条建议**：任何界面改动，先问自己"我妈能不能一眼看懂并点对"。
这个项目里被推翻重做最多的方案，都是"技术上更优雅但老人用不了"的方案。

### 1.4 角色模型

系统有 10 种角色（`enums/UserRole.java`），日常涉及 6 种：

| 层级 | 角色 | 说明 |
|---|---|---|
| 决策层 | 主任、副主任 | 发起会议、公示、结束会议等正式动作 |
| 协作层 | 委员、记录员、业委会秘书 | 参与表决/发言，可编辑草稿，不能发起正式动作 |
| 外部 | 业主 | 只看已公示内容、提交诉求 |
| 外部 | 物业 | **不参与小区行政事务**，看不到任何会议内容，只有公开信息 + 接待反馈 |

> ⚠️ 权限边界不是猜的，写在 `产品边界定稿.md` 里，那份文档是"宪法"。
> 代码与它冲突时，以文档为准。

---

## 二、当前进度

### 2.1 总体状态

**可用的原型/试点版本，尚未正式上线。**
核心业务闭环已跑通，但有一批"测试期临时方案"必须在上线前复原（见 §8.1）。

代码规模（2026-08-04）：

| | 规模 |
|---|---|
| 前端 | 31,783 行，35 个页面，Vue 3 |
| 后端 | 18,284 行，158 个接口，30 张表 |
| 提交数 | 882 次（起始 2026-07-15） |

### 2.2 各模块完成度

| 模块 | 状态 | 说明 |
|---|---|---|
| 会议全流程 | ✅ 闭环可用 | 计划→发起→通知→签到→录音→表决→纪要→公示→归档 |
| AI 语音转写 | ✅ 可用 | 豆包 ASR，支持分段、断点续录、落盘恢复 |
| AI 纪要生成 | ✅ 可用 | 大模型生成，可人工修订并留痕 |
| 业主接待 | ✅ 可用 | 接待日安排、登记、办理、归档 |
| 学习培训 | ✅ 可用 | 发起、通知、签到、结果登记 |
| 待办事项 | ✅ 可用 | 决议转待办、跨会议聚合、闭环 |
| 印章管理 | ✅ 可用 | 申请、审批、留痕 |
| 履职档案/监督 | ✅ 可用 | 四类记录归档检索、统计看板 |
| 区块链存证 | ⚠️ **方案完成，未接入** | 见 `会议纪要上链存证方案.md`，等对方接口 |
| 工单系统对接 | ⚠️ **代码完成，等凭证** | 填三个环境变量即通，见 §8.2 |
| 可信档案馆归档 | ❌ 未开始 | 等对方提供接口规范 |
| 真实登录体系 | ❌ 未做 | 目前是演示身份自由切换，**上线必改** |
| 信创适配 | ❌ 未做 | 如果验收要求信创，工作量另算 |

### 2.3 最近的工作重心（2026-08 上旬）

近期几乎全部精力在**界面适老化打磨**，与设计师逐页对齐。已完成：

- 会议通知页、会议签到页、会议进行页、议题处理页 —— 四页按设计稿重做
- 全局三色制清理（去掉绿/红/橙/墨绿等语义外颜色）
- 可点行统一 54px 下限，箭头全部改 CSS 绘制
- 历史记录页加翻页（每页 6 条）+ 年份筛选

**设计师待办里还剩的**（下一步可以直接接着做）：
1. 第 4 步"会后整理"页尚未按新规范重做
2. 把"放大 20%"的尺寸标准回写进 `全局配色与交互规范-老年友好.md`

---

## 三、代码结构地图

### 3.1 仓库顶层

```
committee/
├── h5/                      前端（Vue 3 + Vite）★ 日常改动主要在这
├── backend/                 后端（Spring Boot 3.2.6 + Java 17）
├── ocr-asr-service/         OCR/ASR 辅助服务（Python FastAPI，:8003）
├── deploy/                  部署（Docker Compose、Nginx 配置、部署文档）
├── docs/                    补充文档（上线前TODO、地图接入、演示小区设定）
├── test-data/               测试数据（历史会议 SQL、模拟会议材料包）
├── CLAUDE.md                ★ 项目约定（踩过的坑，必读）
├── 产品边界定稿.md            ★ 权限与可见性"宪法"
├── 全局配色与交互规范-老年友好.md  ★ 适老化设计规范
├── 业委移动端设计规范.md        页面级设计规范
├── 会议纪要上链存证方案.md      区块链方案（待实施）
├── 敏感信息保护.md / 敏感信息保护-整改清单.md  安全规范与差距
└── external-ticket-api.md   工单系统对接接口文档
```

### 3.2 前端

```
h5/src/
├── pages/          35 个页面，一个路由一个文件
├── components/     13 个共用组件
├── api/            接口封装，按业务分文件
├── composables/    组合式逻辑（录音、ASR 流、AI 任务）
├── utils/          工具（UI 弹层、存储、权限、导航）
├── stores/         全局状态
└── styles/         全局样式与主题
```

**必须先看懂的 4 个文件**（改任何会议相关需求都会碰到）：

| 文件 | 行数 | 说明 |
|---|---|---|
| `pages/Committee.vue` | 最大 | 首页，会议/接待两个 tab 复用同一页面 |
| `pages/CommitteeDetail.vue` | 大 | 会议通知页（发通知、选人、送达状态） |
| `pages/MeetingLiveQuick.vue` | ~4700 | **会议进行向导**，四步全在这一个文件里 |
| `components/TopicSheet.vue` | ~1700 | 议题处理弹层（表决、讨论、通报三种形态） |

⚠️ `MeetingLiveQuick.vue` 是全项目最复杂的文件，含签到、录音、议题、会后整理四个步骤。
改它之前先通读顶部注释——里面记了大量历史决策原因。

**几个关键组合式**：
- `composables/useRecorder.js` —— 录音核心，含暂停/续录/中断恢复/落盘
- `composables/useAsrStream.js` —— 实时语音转文字
- `utils/recStore.js` —— 录音切片落 IndexedDB，防崩溃丢失

### 3.3 后端

```
backend/src/main/java/com/ywh/
├── controller/    18 个，按业务划分
├── service/       15 个根服务 + service/quick/（AI 相关 23 个）
├── entity/        30 张表
├── repository/    JPA 仓储
├── dto/           出参对象
├── config/        安全、JWT、微信、豆包、存储、种子数据
└── enums/         14 个枚举（角色、阶段、议题类型、表决选项…）
```

**核心文件**：
- `service/CommitteeService.java` —— **最大的服务类**，会议全流程逻辑几乎都在里面
- `service/quick/` —— AI 能力：ASR、纪要生成、议题提炼、OCR、转写纠错
- `config/RoleAuthAspect.java` + `annotation/@RequireRole` —— 接口级角色鉴权
- `config/*Seeder.java` —— 演示数据种子，上线前要清理

### 3.4 数据模型（30 张表的主线）

```
Community（小区）
 └─ UserRoleEntity（人 + 角色）
     └─ CommitteeMeeting（会议）
         ├─ MeetingDelivery（通知送达：谁收到了、读没读）
         ├─ MeetingNotificationLog（通知发送日志）
         ├─ MeetingMaterial（会议材料）
         ├─ MeetingRecord（会议记录，纪要正文在这）
         │   ├─ RecordAttendance（签到/出席）
         │   ├─ RecordTopic（议题）
         │   │   ├─ TopicVote（逐人投票）
         │   │   └─ TopicOpinion（意见发言）
         │   └─ MeetingRecording（录音分段 + 转写结果）
         ├─ MeetingTodo（会议决议转待办）
         ├─ MinutesRevision（纪要版本快照）
         └─ MeetingPublish（公示留痕，上链回执将来挂这）
```

其余独立主线：`ReceptionRecord`（接待）、`LearningRecord`（学习）、`SealUseRecord`（印章）、`WorkItem`（事项）。

---

## 四、怎么跑起来

### 4.1 依赖

| 组件 | 版本 | 端口 |
|---|---|---|
| Node.js | 18+ | 前端 5173 |
| JDK | 17 | 后端 8080 |
| Maven | 3.9 | — |
| MySQL | 8.x | 3306 |
| Python | 3.12+ | OCR/ASR 8003 |

### 4.2 启动命令

```bash
# 前端
cd h5 && npm install && npm run dev        # → http://localhost:5173

# 后端
cd backend && mvn spring-boot:run           # → http://localhost:8080

# OCR/ASR 服务（需要 OCR 或语音功能时才启）
cd ocr-asr-service && ./entrypoint.sh       # → :8003
```

Windows 上可以用根目录的 `dev-start.ps1` 一次拉起后端两个服务
（注意：里面写死了作者机器的 Python/Maven/JDK 路径，**接手后要改成你自己的**）。

### 4.3 配置文件（关键）

后端配置**不在版本库里**，需要自己建：

```bash
cp backend/src/main/resources/application.yml.example backend/src/main/resources/application.yml
cp backend/.env.example backend/.env
```

`.env` 里需要填的密钥（**向交接人索取，不要在群里发**）：

| 环境变量 | 用途 |
|---|---|
| `DB_PASSWORD` | 数据库密码 |
| `JWT_SECRET` | 登录令牌签名 |
| `DOUBAO_ASR_APP_KEY` / `ACCESS_TOKEN` / `SECRET_KEY` | 语音转写 |
| `DOUBAO_LLM_API_KEY` | 大模型（纪要生成） |
| `OCR_SERVICE_TOKEN` | OCR 服务鉴权 |
| `STORAGE_TOS_ACCESS_KEY` / `SECRET_KEY` | 对象存储（录音文件） |
| `EXTERNAL_TICKET_*` | 工单系统对接（目前未启用） |

前端配置在 `h5/.env.local`：`VITE_API_BASE`、`VITE_AMAP_KEY`（地图）等。

> ⚠️ 地图目前用的是**个人开发者测试 Key**，上线必须换成企业 Key（见 `docs/上线前TODO.md`）。

### 4.4 数据库

- **Flyway 关闭**，用 `ddl-auto: update` 自动建表
- ⚠️ **新增字段必须可空**，否则启动时加列会失败
- 改了实体类**必须重启后端**才生效
- 演示数据由 `config/*Seeder.java` 自动灌入
- 历史会议测试数据：`test-data/seed-history-meetings.sql`

### 4.5 测试账号

目前是**演示模式**：登录页/个人中心可以自由切换 8 个身份，无需密码。

| ID | 姓名 | 角色 |
|---|---|---|
| 1 | 张建国 | 主任 |
| 2 | 李秀英 | 副主任 |
| 3-7 | 王志强、赵丽娟、刘海涛、陈晓梅、杨国华 | 委员 |
| 8 | 秘书小李 | 业委会秘书 |

> ⚠️ 这是测试期方案。上线前必须换成真实账号体系，并关闭 `dev-token` 直通鉴权。

---

## 五、必须先知道的业务规则

### 5.1 会议的生命周期

```
preparing（准备）→ ongoing（进行中）→ ended（已结束）
```

- 发起会议后进 `preparing`，可以改内容、发通知
- 点"开始会议"进 `ongoing`，开始签到/录音/表决
- 会后整理完成进 `ended`，之后才能公示

⚠️ **测试期有个坑**：`MeetingLiveQuick.vue` 和 `Minutes.vue` 各有一个 `TEST_KEEP_MEETING_OPEN = true`，
让"完成会后整理"跳过真正的结束动作，会议一直留在进行中（方便反复测试）。
**上线前两处都要置回 false**，否则公示流程走不通。

### 5.2 公示规则（"宪法"里最重要的一条）

> **未公示的纪要只对治理角色可见；公示后普通业主可见；公示后不允许静默修改，只能修订或撤回。**

- 公示是正式动作，记录公示人、时间、状态；撤回同样留痕
- 物业**任何时候都看不到**会议内容
- 详见 `产品边界定稿.md`

### 5.3 议题与表决

议题分三类（`TopicType`）：
- **表决类**（decision/major/ordinary）—— 要投票，"我处理完了"= 我投过票
- **通报类**（notice）—— 要确认收到，"我处理完了"= 我点过"我已收到"
- **讨论类**（discussion）—— **进过页面就算完成**，不强制发表意见

⚠️ 会议进行页的"已处理 N/M"判据是**"我自己填完了没"**，不是"议题达成结果没"。
原因：表决要求赞成票过全体委员半数，7 人缺 2 人时可能永远达不到，
用"结果"当判据会让主任永远结束不了会议。这个坑踩过，别改回去。

### 5.4 适老化设计规范（改界面前必读）

完整规范在 `全局配色与交互规范-老年友好.md`，核心几条：

**三色制**
- **蓝** `#3567A4`（会议模块主色）/ `#2f5f9e`（次）/ `#EAF0F8`（浅底）
- **中性灰** —— 常态、状态、结果
- **暖橙** `#f7e4c6` / `#9a5b12` —— **只用于异常态**（逾期、录音中断预警），一屏最多一处

绿色是接待模块专用色，会议模块不能出现；红色除删除外不用。

**尺寸下限**
- 可点击的列表行 ≥ 54px
- 主按钮 ≥ 54px
- 议题选项等大目标 68px

**其他硬性约定**
- 下拉/展开箭头**不能用 `⌄` `⌃` 字符**（基线偏低，垂直居中压不准），一律用 CSS 边框画
- 底部主操作条必须 `position: fixed` 钉在底栏上沿，不能放文档流
- 可点的行用原生 `<button>`，不要用 `<div @click>`（有 300ms 延迟 + 长按会选中文字）

---

## 六、踩过的坑（省时间用）

这一节的内容都写在 `CLAUDE.md` 里，接手后**第一件事就是读那个文件**。这里挑最容易再犯的：

### 6.1 前端

**Vue `watch()` 会立即求值 → TDZ 报错整页白屏**
```js
// ❌ setup 顶层这样写，如果 computed 链里引用了后面才声明的 const，直接白屏
watch(footerStage, () => {...})
// ✅ 放进 onMounted
onMounted(() => { watch(footerStage, () => {...}) })
```
computed 是惰性的所以没事，watch 是立即的，这个区别害过一次整页打不开。

**滚动只在 `#app-scroll`，window 上的滚动 API 全部失效**
```js
// ❌ window.scrollTo(0, 0)
// ✅ document.getElementById('app-scroll').scrollTo(0, 0)
```
因为 `html/body` 设了 `overflow:hidden`（手机浏览器地址栏伸缩会把 fixed 底栏带走）。

**CSS 特异性**：同权重后写的赢。有过"按钮该置灰却是蓝色"的 bug，
原因是灰色规则写在蓝色规则前面，两者都是 (0,4,0)。

**禁用按钮不触发 click**：如果按钮禁用时还需要弹提示（"请先完成本项表决"），
不能用 `disabled` 属性，要用样式类表达禁用状态。

### 6.2 后端

- 改了实体/服务**必须重启**才生效
- 新增列**必须可空**（Flyway 关闭，靠 ddl-auto 加列）
- 表决类议题的 `status` 是**实时算出来的**，不是存的：赞成票 ≥ 过半全体委员才是 `passed`

### 6.3 工程

- 提交前先 `cd` 到仓库根目录（在 `h5/` 里 `git add h5/src/...` 会 pathspec 不匹配）
- 前端构建：`cd h5 && npm run build`
- 后端离线编译：`mvn -f backend/pom.xml -q compile -o`

---

## 七、协作流程

### 7.1 分支

日常在 `claude/homepage-card-layout-dwi775` 开发，**同一个 HEAD 推两个分支**：

```bash
git push -u origin claude/homepage-card-layout-dwi775
git push origin HEAD:codex-local-work
# 验证两边同步：应输出 0 0
git rev-list --left-right --count origin/codex-local-work...HEAD
```

> 接手后可以和团队确认是否还需要维持双分支——这是历史上多个协作方并行留下的做法。

### 7.2 需求来源

需求主要来自两条线：

1. **设计师**：以聊天截图 + 设计稿（`.dc.html` 模拟文件）的形式给意见，通常一次 3-5 条
2. **codex（并行的代码审查）**：提交问题清单，**必须先对照代码核实再改**——
   历史上出现过多次"报的问题在当前代码里不存在"（对方看的是旧版本）

### 7.3 验证方式（强烈建议延续）

这个项目**没有单元测试**，靠的是 Playwright 无头浏览器实测：

```javascript
// 典型用法：拦截 API 返回构造数据 → 打开页面 → 量真实像素/文案
import { chromium } from '/opt/node22/lib/node_modules/playwright/index.mjs'
const browser = await chromium.launch({ executablePath: '/opt/pw-browsers/chromium' })
await page.route((u) => u.pathname.startsWith('/api/'), route => route.fulfill({...}))
```

要点：
- 路由拦截用 `(u) => u.pathname.startsWith('/api/')`，**不要用 `**/api/**`**（会拦截 vite 的模块 URL 导致页面加载失败）
- 需要预置 `sessionStorage.demo_authed = '1'` 才能过路由守卫
- 录音相关测试要加 `--use-fake-device-for-media-stream`

这套方法能量出"这个按钮到底多少 px""这个颜色到底是不是三色制内"，
比肉眼看截图可靠得多，也是很多设计师意见能被证伪/证实的原因。

**建议补上的**：Playwright 的 `toHaveScreenshot()` 做视觉回归 + `axe-core` 做无障碍自动检查。
现在每次改动都要人工看截图，这块可以自动化。

---

## 八、待办与风险

### 8.1 上线前必办（完整清单见 `docs/上线前TODO.md`）

**🔴 安全相关（必须做，否则不能上线）**
1. **身份切换改真实登录** —— 现在 8 个演示身份可自由切换（`Profile.vue` 的 `allIdentities`）
2. **关闭 dev-token 鉴权直通** —— 前端无 token 时用 `dev-token-<roleId>` 直接过（`h5/src/api/core.js`）
3. **关闭 `/api/auth/dev-roles`** —— 免登录暴露委员姓名（`SecurityConfig` permitAll）
4. **地图 Key 换企业版** —— 现用个人开发者测试 Key

**🟡 功能开关复原**
5. `TEST_KEEP_MEETING_OPEN = false`（两处：`MeetingLiveQuick.vue`、`Minutes.vue`）
6. 首页会议卡片的测试期过滤逻辑复原（`Committee.vue` 的 `loadAll`）
7. 代投凭证是否恢复必填（防"主任替人乱投"的审计要求）
8. 清理演示数据种子（`config/*Seeder.java`）

**🟢 待改进**
9. 敏感信息：手机号等 PII 目前明文存储、无掩码（见 `敏感信息保护-整改清单.md`）
10. 超长录音识别预算：单段超约 1.5 小时可能误报超时

### 8.2 等外部方的事（不是代码问题）

| 事项 | 卡在哪 | 我方准备情况 |
|---|---|---|
| 工单系统对接 | 等对方分配 UID 并在其系统登记 | ✅ 代码完整，填 3 个环境变量即通 |
| 可信档案馆归档 | 等对方提供接口规范 | ⏳ 公示流程已就绪，差归档调用 |
| 区块链存证 | 等确定链的选型/服务商 | ✅ 方案文档完整，SHA-256 工具已有 |

### 8.3 需要重启后端才生效的近期改动

交接时如果测试环境后端未重启，以下功能表现异常属正常：

1. 讨论类议题"进过页面即完成"的留痕（未重启则议题永远停在"去讨论"）
2. 签到接口的阶段与资格校验
3. 通知失效标记（`notice_stale` 字段）

### 8.4 技术债

- `MeetingLiveQuick.vue` 4700 行，四个步骤挤在一个文件，建议按步骤拆分
- `CommitteeService.java` 是巨型服务类，建议按业务域拆
- 无自动化测试，全靠人工 + Playwright 脚本
- 前端无状态管理规范，部分状态散在页面里

---

## 九、交接清单（逐项打勾）

### 9.1 代码与文档
- [ ] Git 仓库访问权限（GitHub: `rogerali542-cpu/committee`）
- [ ] 说明当前工作分支与双分支推送约定
- [ ] 确认已阅读：`CLAUDE.md`、`产品边界定稿.md`、`全局配色与交互规范-老年友好.md`

### 9.2 密钥与账号（**不要通过聊天工具传**）
- [ ] `backend/.env` 全部密钥（豆包 ASR / LLM、TOS 存储、JWT、数据库）
- [ ] `h5/.env.local`（地图 Key）
- [ ] 服务器 SSH / 部署账号
- [ ] 数据库账号
- [ ] 域名/DNS 管理入口（当前部署域名见 §9.4）

### 9.3 外部联系人
- [ ] 设计师联系方式
- [ ] 工单系统对接方（等对方 UID）
- [ ] 档案馆/区块链对接方
- [ ] 甲方/街道对接人

### 9.4 环境信息
- [ ] 测试环境地址与访问方式
- [ ] 生产/演示环境部署方式（见 `deploy/README-部署.md`）
- [ ] 已知：部署域名为 `u-trustcommitte.slaicreativity.com`（注意 committe 是一个 e，不是拼写错误）

### 9.5 建议的交接会内容
1. 现场跑一遍完整会议流程（发起 → 通知 → 签到 → 录音 → 表决 → 纪要 → 公示）
2. 一起看一遍 `MeetingLiveQuick.vue` 的四个步骤
3. 演示 Playwright 验证脚本怎么写怎么跑
4. 过一遍 §8.1 上线前清单，确认理解每条的原因

---

## 十、给接手人的三句话

1. **先读 `CLAUDE.md` 和 `产品边界定稿.md`**，这两份文档能省掉你至少一周的试错。
2. **改界面前先问"老人能不能用"**，这个项目所有被推翻的方案都栽在这一点上。
3. **代码里的中文注释很多是决策记录**（"0803 用户定""设计师二改"），
   看到反直觉的写法先读注释——大概率是踩过坑之后的结果，不是随手写的。

祝顺利。有问题随时找我。

---

*文档生成日期：2026-08-04*
