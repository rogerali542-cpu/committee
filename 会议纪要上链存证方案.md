# 会议纪要上链存证 · 技术方案

> 目标：结束的会议在**公示**的同时，把会议纪要"上链"，实现**防篡改、可追溯、可验真**的可信公示。
> 版本：v1（2026-07-03 调研稿）

---

## 一、核心结论（先看这一条）

**不把纪要全文塞进区块链，而是"哈希存证"。**

- 链上只写一条**存证记录**：纪要正文的 `SHA-256` 摘要 + 会议元数据（会议号 / 标题 / 时间 / 参会人数 / 版本哈希） + 上链时间戳；
- 正文全文仍存在我们自己的数据库（现有 `MeetingRecord.minutesText`）；
- 公示页展示：**纪要正文 + 上链凭证**（交易哈希 `txHash`、区块高度、存证编号、上链时间） + **「一键验真」**（现场重算 hash 与链上比对，证明未被篡改）。

**为什么这样做**：链上存储贵、且不可删除会带来隐私/合规风险（业主姓名等）；哈希存证是行业标准做法（司法存证、电子证据平台都这么做），成本极低，同样满足"防篡改可追溯"的公示诉求。

---

## 二、现状盘点（已有的可复用资产）

调研发现这个功能是"顺水推舟"，因为底座已经具备：

| 已有能力 | 位置 | 复用方式 |
|---|---|---|
| **公示流程 + 留痕表** `MeetingPublish`（公示人/时间/撤回原因） | `entity/MeetingPublish.java`；`CommitteeService.advanceStage` 318-326、`getPublishInfo` 2517-2547 | 上链回执**直接加字段挂在这张表**，跟着公示动作走 |
| **SHA-256 工具** `sha256()` | `CommitteeService.java` 1735-1746 | 直接算纪要正文哈希 |
| **纪要正文** `minutesText`（TEXT） | `entity/MeetingRecord.java` 34-35 | 存证的原文来源 |
| **纪要版本快照** `MinutesRevision` | entity 层 | 上链时锁定的正是某一版快照 |
| **外部 HTTP 调用范式** `NewsGenService`（`java.net.http.HttpClient` + `DoubaoProperties` 配置 + 失败 fallback） | `service/quick/NewsGenService.java` 31-100；`config/DoubaoProperties.java` | **照抄这个范式**写上链服务与配置类 |
| **前端公示区 UI** 「公示会议 / 已公示 / 撤回公示 / 查看公示纪要」 | `pages/CommitteeDetail.vue` 227-299 | 在此处加"上链凭证"展示 + 验真按钮 |

**结论**：无需新造流程。上链 = 给"公示"动作加一个钩子。

---

## 三、技术选型（已定：对接公司「小区可信档案馆 V2.0」）

**社区链已确定** = 公司现有产品 **「小区可信档案馆 V2.0」**（基于区块链的小区数据公示与管理平台，操作手册见 `../../小区可信档案馆V2.0操作手册.html`）。可信级别"跟现有一致"，因此**不自建、不接第三方 SaaS**，一律走可信档案馆。

### 可信档案馆现状（据操作手册）
- 机制 = **哈希存证**：文件上链后带「已上链/未上链」标识；已上链 = 写入区块链**不可篡改、不可删除**；点开可见 **上链人 / 哈希值 / 上链方式 / 上链时间 / 上链费用 / 数据来源 / 公示人 / 公示时间 / 关联小区 / 文件类型** 等——与本方案第四节的凭证字段高度吻合。
- **「会议纪要」本就是它「链上公示 · 第4章 文件公示」支持的文件类型之一**（同类还有新闻稿、服务合同、履职报告、收费标准、办事制度…）。
- 手册描述的是一个**人工操作的 Web 平台**：登录→选小区/角色→上传文件→选存证/公示选项→完成上链。**手册中未见对外 REST API**。

### 集成方式（✅ 已定：方式一 · API 自动推送）

> 2026-07-03 确认：可信档案馆**有内部 API**，可自动推送 → 采用**方式一**。方式二/Mock 作为兜底与过渡态保留。

**✅ 方式一 · API 自动推送（本方案采用）**
- 我方后端在"公示"时，把纪要文件（或哈希+元数据）**自动推送**到可信档案馆的上链接口，回执落库。
- 用户在业委会 App 里点一次"公示"即完成上链，最省事、最符合"老人易用"。
- **前提**：需要档案馆提供内部 API（地址 / 鉴权方式 / 文件类型枚举 / 小区ID映射）。手册未含，需向档案馆团队索取。

**方式二 · 导出 + 引导人工上传（无 API 时的稳妥兜底）**
- 我方 App 会议结束→一键把纪要生成**规范 PDF/文件**并导出；引导主任登录可信档案馆，按第4章上传该文件完成上链公示。
- 零对接、零风险，但多一步人工操作。
- 可加"上链回执登记"：主任上传后把档案馆给的哈希/上链时间**回填**到我方 App，App 侧照样展示凭证 + 验真。

### 过渡态 · 本地 Mock（P0，无论走哪种方式都先做）
- 后端先实现 `MockChainAnchorService`（`SHA-256 + 递增区块号 + 服务端签名`，回执落库），**零外部依赖即可全栈跑通 UI 与流程**，等档案馆对接信息到位再换真实适配器。

> **架构统一**：定义 `ChainAnchorService` 接口，`mock | archive-api | manual` 各一个实现，配置 `chain.provider` 切换；业务代码只认接口，换实现不改业务。

**➡️ 需要你确认**（见文末问题清单）：可信档案馆到底有没有可调用的内部 API？没有就走"方式二 导出+回填"。

---

## 四、上链存证载荷设计（链上写什么）

存证记录（JSON，算哈希后上链；链上存哈希 + 精简元数据）：

```json
{
  "bizType": "committee_meeting_minutes",
  "meetingId": 193,
  "meetingNo": "2026年第8次业委会例会",
  "committeeId": 1,
  "minutesSha256": "<纪要正文 minutesText 的 SHA-256>",
  "minutesRevisionId": 42,          // 锁定的版本快照
  "topicsDigest": "<议题+表决结果的 SHA-256>",
  "attendeeCount": 7,
  "publishedBy": "张建国",
  "publishedAt": "2026-07-03T14:20:00+08:00"
}
```

- 链上真正写入：`docHash = SHA-256(上面这段 canonical JSON)` + 少量明文元数据（meetingId、时间、bizType）。
- **不写业主个人隐私明文**（姓名等只进入哈希，不单独明文上链）。

---

## 五、后端改动清单

### 5.1 数据库：给 `MeetingPublish` 加"上链回执"字段（ddl-auto:update 自动建列，无需迁移）
```
chain_status        VARCHAR   -- none|pending|anchored|failed
chain_provider      VARCHAR   -- mock|archive（可信档案馆）
chain_doc_hash      VARCHAR(64) -- 上链的存证哈希
chain_tx_hash       VARCHAR   -- 交易哈希
chain_block_height  BIGINT    -- 区块高度
chain_cert_no       VARCHAR   -- 档案馆返回的存证编号
chain_anchored_at   DATETIME  -- 上链时间
chain_verify_url    VARCHAR   -- 档案馆公示页/可验证链接（若返回）
```

### 5.2 配置类 `ChainProperties`（照抄 `DoubaoProperties` 模式）
```
chain.enabled            = false        # 总开关
chain.provider           = mock         # mock|archive
chain.archive.baseUrl    = http://...   # 可信档案馆内部 API 地址（待档案馆提供）
chain.archive.appId      = ...          # 鉴权（形式待接口文档确认）
chain.archive.secret     = ...          # 密钥（走环境变量，不入库）
chain.archive.fileType   = meeting_minutes  # 「会议纪要」在档案馆的类型枚举值
```
> 密钥走环境变量 / `.env`，**绝不入库、绝不提交 git**（沿用项目现有铁律，只提交 `.env.example`）。

### 5.3 服务 `ChainAnchorService`（接口 + 两实现，HTTP 部分照抄 `NewsGenService.callLlm`）
```java
interface ChainAnchorService {
    AnchorReceipt anchor(AnchorPayload payload);   // 上链，返回 txHash/blockHeight/certNo...
    VerifyResult  verify(Long meetingId);          // 取链上哈希，与当前纪要重算比对
}
```
- `MockChainAnchorService`（P0，零依赖，本地模拟回执）
- `ArchiveChainAnchorService`（P1，HTTP 调可信档案馆 API，10s 连接 / 120s 请求超时，失败 fallback → chain_status=failed 可重试）

### 5.4 触发时机：挂在"公示"动作上
- 主任点「公示会议」→ 现有公示逻辑执行成功后 → **异步**调 `chainAnchorService.anchor(...)`（异步，避免链慢阻塞公示；先落 `pending`，回执到了改 `anchored`）。
- 失败不阻断公示，`chain_status=failed`，前端给"重试上链"入口。

### 5.5 新增接口
```
POST /api/committees/{id}/anchor      # 手动触发/重试上链（幂等：已 anchored 直接返回回执）
GET  /api/committees/{id}/verify      # 一键验真：重算 hash → 与链上比对 → 返回 一致/不一致 + 凭证
```
- `getPublishInfo` 的返回 VO 里带上 chain 回执字段，供前端展示。

---

## 六、前端改动清单

### 6.1 公示区（`CommitteeDetail.vue` 227-299，主任视图）
- 已公示卡片下增加**「上链凭证」小卡**：存证编号 · 交易哈希（截断显示，可复制）· 区块高度 · 上链时间；状态徽标（🟢已上链 / 🟡上链中 / 🔴上链失败·点击重试）。
- `chain_status=failed` 时给"重新上链"按钮（调 `POST /anchor`）。

### 6.2 公开纪要页（委员/业主"查看公示纪要"）
- 底部加**「区块链存证」**版块：一句人话说明（"本纪要摘要已存证于XX链，不可篡改"）+ 凭证信息 + **「一键验真」按钮**。
- 验真结果用大字/绿勾红叉给老年用户看得懂："✓ 内容与链上一致，未被篡改" / "✗ 内容与存证不一致"。
- 可选：生成一个**验证二维码**（扫码到第三方链浏览器/验证页），公示打印件上也能贴。

### 6.3 文案（面向老人，避免术语）
- 不说"哈希/交易"，主按钮就叫**「查看存证 / 验证真伪」**；技术细节折叠在"详情"里。

---

## 七、验真流程（防篡改的核心价值）

```
用户点「一键验真」
  → 后端取当前 minutesText，重算 SHA-256
  → 取链上该会议的 chain_doc_hash（或直接查链）
  → 比对：一致 → ✓ 未被篡改（附上链时间、区块高度）
          不一致 → ✗ 内容已变更（说明纪要在上链后被改过 / 或对比的是新版本）
```
> 这解释了"为什么要上链"：任何人（包括我们自己）改了纪要，验真都会失败，从而**不可抵赖**。

---

## 八、分阶段实施

| 阶段 | 内容 | 外部依赖 | 产出 |
|---|---|---|---|
| **P0（1天）** | Mock 链跑通全链路：DB 字段 + `ChainProperties` + `MockChainAnchorService` + anchor/verify 接口 + 前端凭证卡 + 验真按钮 | 无 | 全栈可演示，UI/流程定稿 |
| **P1（视档案馆而定）** | 接可信档案馆：有 API→`ArchiveApiChainAnchorService` 自动推送；无 API→纪要一键导出 PDF + 回执回填 | 需档案馆 API 文档 / 或导出规范 | 真实上链、真实验真 |
| **P2（可选）** | 验证二维码 / 跳档案馆公示页 / 议题表决结果与补充材料一并存证 | 依档案馆能力 | 增强可信与对外展示 |

**建议先做 P0**：无论档案馆最终走 API 还是人工上传，P0 的数据模型、接口、前端全部可复用，真实对接只是替换一个 Service 实现。

---

## 九、安全 / 隐私 / 合规

- **隐私**：个人信息只进哈希、不明文上链；链上不可删，务必确认载荷里没有不该公开的字段。
- **密钥**：档案馆 API 的 appId/secret 走环境变量，`.env` 不提交 git（沿用项目铁律）。
- **一致性**：上链锁定的是某一版 `MinutesRevision` 快照；公示后若修订纪要，应走"撤回→重新公示→重新上链"（现有撤回留痕机制已支持），并保留旧存证形成可追溯链条。
- **可用性**：链故障不能阻断公示——异步上链 + 失败可重试，公示本身永远先成功落库。

---

## 十、需要你确认的问题清单

> 链已定为「小区可信档案馆 V2.0」，可信级别跟现有一致，集成走「API 自动推送」。**开工前需向档案馆团队索取以下接口信息**：

1. **接口文档**（必需）：上链/存证接口的 **URL、请求方法、鉴权方式**（Token/AppId+Secret/内部网关？）、**请求体字段**、**返回体字段**（务必含 txHash/哈希/上链时间/存证编号/公示页链接）。
2. **枚举与映射**：文件类型枚举中「会议纪要」对应的值；我方小区/委员会 ID → 档案馆「小区ID」的映射关系。
3. **上传形态**：是"传文件（PDF）"还是"传哈希+元数据 JSON"？若传文件，接受哪些格式/大小上限。
4. **查询/验真接口**：能否按存证编号或哈希反查链上记录（供 App「一键验真」用）。
5. **测试环境**：有无 sandbox/测试小区与测试账号，便于 P1 联调不污染正式链。

> 另需你拍板两点（不阻塞出方案）：**上链粒度**（仅纪要正文 / 含议题表决结果 / 含补充材料）、**触发方式**（绑定"公示"自动触发【推荐】/ 主任单独按钮手动触发）。

---

*先落地 P0（Mock 全链路）可立即演示；真链信息到位后 P1 换适配器即可。*
