# H5 适老化改革 — 基线记录与方案

> 记录日期：2026-07-20 ｜ 基线提交：`19f574f` ｜ 分支：`claude/software-weekly-progress-x5rdax`
> 目标：面向退休业主群体，把 H5 端整体做到「看得清、点得中」。
> 适老基线：**正文 ≥16px、点击区 ≥44px（88rpx）、文字对比度 ≥4.5:1**。

---

## 一、改革前基线（当前状态快照）

范围：`h5/src` 下 33 个 `.vue`（全部 `<style scoped>`）+ `styles/global.css`。
单位说明：`rpx` 经 postcss 按 `750rpx = 100vw` 转 vw；在 375px 屏 **1rpx ≈ 0.5px**（下文 px 均按此换算）。

### 1. 字号现状（共 1363 处 font-size 声明）
主体严重偏小，14px 及以下占绝大多数：

| rpx | ≈px | 出现次数 | 适老判定 |
|----:|----:|-------:|---|
| 28rpx | 14px | **446** | ✗ 低于 16px 正文线（且是全局 body 基线） |
| 30rpx | 15px | 155 | ✗ 偏小 |
| 32rpx | 16px | 109 | ✓ 达标 |
| 26rpx | 13px | 88 | ✗ |
| 24rpx | 12px | 66 | ✗ |
| 22rpx | 11px | 29 | ✗ 极小 |
| 23/25/27/29rpx | 11.5–14.5px | ~110 | ✗ |

全局 body 基线 = `28rpx`（14px，`global.css:33`）。

### 2. 对比度现状（541 处硬编码灰 vs 317 处令牌引用）
大量浅灰文字对比度不足（白底上 <4.5:1）：

| 硬编码值 | 次数 | 白底对比 | 判定 |
|---|---:|---:|---|
| `#999` | 27 | ~2.85:1 | ✗ |
| `#8a8f98` | 26 | ~3.5:1 | ✗ |
| `#9aa0a6` | 20 | ~3.0:1 | ✗ |
| `#888` | 12 | ~3.5:1 | ✗ |
| `#8a9099` | 6 | ~3.7:1 | ✗ |
| `#aaa` / `#bbb` / `#b7bbc0` / `#9a9a9a` / `#a0a5ad` / `#8d949d` | 各 2–3 | 1.9–3.0:1 | ✗ |
| 低透明度文字 `rgba(...,0.28/.32/.35/.4)` | 8 | 低 | ✗（如发起页推荐标题 32% 透明） |

> 说明：橙/红/绿等品牌与语义色（`#c76a00`/`#a85800`/`#c0392b`/`#1e7e4e` 等）本身较深、对比达标，**不在本次改动范围**。只改「中性浅灰文字」。

### 3. 点击区现状
交互元素高度分布里，仍有相当比例 <88rpx（44px）：`60rpx`(30px)、`56rpx`、`48rpx`、`44rpx`(22px)、`36rpx`、`34rpx` 等。
典型偏小：发起页 `type-chip` 30px、线下/线上切换按钮 ~27px、平台下拉 32px。

### 4. 首个复查页「发起业委会」具体问题（样式 3950–4390）
- 🔴 推荐标题 `.title-ghost` 32% 透明（4386）；占位 `.fl-value.ph` `#b7bbc0`（4005）；`.ds-s` `#999`+11px（4048）。
- 🟠 字段标签 `.fl-label` 13px（4003）、选中值 `.fl-value` 14px（4004）、`.type-chip` 14px（4361）。
- 🟠 `.type-chip` 30px、线下/线上 ~27px、平台下拉 32px 点击区偏小。
- ⚪ 功能：`会议说明` 字段数据层保留但手动表单无输入入口（仅扫描识别/编辑旧会议带出）。

---

## 二、改革方案

### 令牌层（新增，供后续统一引用）
在 `:root` 增补适老语义令牌：
- `--fs-body: 32rpx`（16px 正文线）、`--fs-sub: 30rpx`、`--fs-cap: 28rpx`、`--fs-tiny: 26rpx`(13px 下限)。
- `--tap-min: 88rpx`（44px 点击区下限）。
- 收紧弱文字令牌：`--c-text-weak` 由 `#5C6672` 保持（达标）；新增 `--c-text-faint: #6B7078`（≈4.6:1）作为「最浅可用文字」下限。

### 轴 1 · 对比度（全局脚本，零布局风险）
把所有「中性浅灰文字」clamp 到 ≥4.5:1 的下限灰，逐值映射（仅中性灰，不碰暖橙/红/绿）：
```
#999 #9a9a9a #9aa0a6 #a0a5ad #aaa #b7bbc0 #bbb  → #6b7078
#8a8f98 #8a9099 #8d949d #888                    → #5f636b
rgba(0,0,0,.28~.4) / rgba(31,32,36,.32) 文字     → 提到 0.6+ 或改实色 #5f636b
```

### 轴 2 · 字号下限（全局脚本，单次哈希映射避免级联）
仅上抬 <16px 的档位，≥32rpx 不动：
```
22,23,24 → 26 (13px 下限)
25,26,27 → 28 (14px)
28,29,30 → 32 (16px 正文线)   ← 把主力 body 从 14px 提到 16px（核心）
31       → 32
```
body 全局基线 `28rpx → 32rpx`。

### 轴 3 · 点击区（针对性，非盲改高度）
共享交互组件 + 发起页把 `type-chip`、线下/线上按钮、平台下拉、`title-clear`/`×` 等抬到 `min-height: 88rpx`（或就近安全值）。

### 验证
- 本地 `npm run build` 通过。
- 发起业委会、两个首页、会议详情、纪要 目测复查。
- 由用户视觉验收，按反馈微调（预期个别紧凑行需回调）。

---

## 三、执行进度
- [x] 记录基线（本文件）
- [x] 令牌层增补（`--c-text-faint` + `--fs-*` + `--tap-min`，body 基线 28→32rpx）
- [x] 轴 1 对比度全局扫替（106 处中性浅灰 → `#6b7078`/`#5f636b`，达标 ≥4.5:1）
- [x] 轴 2 字号下限全局扫替（1298+ 处；正文 14→16px，小标签 14px 下限，极小信息 13px 下限）
- [x] 轴 3 点击区针对性提升（发起页 type-chip 30→42px、线下/线上 27→36px、平台下拉 32→44px、地图键 30→38px；title-ghost 32%透明→实色）
- [x] `npm run build` 通过（32 文件改动，两次构建均 ✓）
- [ ] **用户视觉验收**（改完待看，按反馈微调紧凑行）

### 首轮改动量
- 32 个 `.vue` + `global.css` + `publish-theme.css`
- 颜色对比修复 106 处；字号上抬约 1310 处；发起页点击区 4 处 + ghost 文字 1 处
- 保留：品牌暖橙/语义红绿（本就达标）、图标内字形（16–19rpx 勾选符/微信卡缩略图，放大会撑破容器）、深色党建主题的半透明描边

### 已知需人工复查的风险点（正文 14→16px 后可能变紧）
- 首页计划表 `period-*` 期数/状态小字（原 20–21rpx 已提到 13px）
- 会议详情 `recipient-card` 通知人员卡多行摘要
- 各处 badge/pill 单行文字是否换行

</content>
</invoke>
