# 依赖
node_modules/

# 本地工具/日志
*.log
.codex-static-server*.js
.codex-static-server*.log
project.private.config.json
miniapp/.docx

# 本地 agent 配置与记忆
.claude/

# 后端录音/音频数据（可能含隐私，勿提交）
backend/data/

# 构建产物
backend/target/

# 部署：真实密钥 env、导出的镜像包、数据库导出（切勿提交；.env.example 可提交）
# 用 deploy/** 广覆盖 docker/ 与 server-bundle/ 等所有子目录，避免漏网
deploy/**/.env
deploy/**/images.tar
deploy/**/*.sql
deploy/**/db-seed/
# 内联了真实密钥、供平台上传的 compose（由 make-deploy-compose.sh 生成，切勿提交）
deploy/**/docker-compose.deploy.yml

# IDE
.vscode/
# 本地设计稿/临时产物
tmp/
ocr-test.png
# 小程序原版（迁移到 H5 前的备份，仅本地参考）
miniapp-original-wx/
miniapp-original-wx.zip
# Office 临时锁文件
~$*.docx
~$*.xlsx
