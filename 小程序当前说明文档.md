# 业委专区小程序当前说明文档

更新时间：2026-06-23

## 1. 当前运行方式

当前小程序已切换为真实后端模式：

- 小程序接口基址：`http://127.0.0.1:8080`
- mock 开关：`miniapp/utils/api/core.js` 中 `USE_MOCK = false`
- 后端服务：Spring Boot，默认端口 `8080`
- 数据库：本机 MySQL，配置见 `backend/src/main/resources/application.yml`

开发测试时需先启动后端，再在微信开发者工具中重新编译小程序。登录页使用开发身份登录，登录后会写入 `dev-token-<roleId>`，后端通过该 token 识别当前角色。

如果出现 `403`，优先检查是否已经重新登录；必要时清理微信开发者工具缓存后再登录。

## 2. 当前产品边界

当前版本以“业委会内部治理”为主，核心模块是业主委员会会议、通知送达、会议记录、会议纪要、公示、接待记录、学习培训、资料库和通知中心。

业主大会相关页面和历史表仍保留在代码中，但当前产品边界中属于历史兼容/停用方向，不作为主要演示和测试入口。

物业不参与小区行政事务，不应看到业委会内部会议流程。物业侧当前主要用于接待反馈和公开信息查看。

## 3. 角色说明

当前开发登录页提供以下测试角色：

| 角色 | 主要能力 |
| --- | --- |
| 主任 | 新建会议、编辑准备阶段会议、发送通知、开始/结束会议、公示、撤回公示、取消准备阶段会议 |
| 副主任 | 与主任类似，协助推进会议流程 |
| 委员 | 查看本人相关会议通知，确认参会，参与表决，查看会议材料和已公开内容 |
| 记录员 | 协助送达、记录、佐证、代录和材料维护，具体权限以页面入口和后端注解为准 |
| 业主 | 查看依法公示后的公开内容 |
| 物业 | 处理物业类接待反馈，查看允许公开的信息 |

## 4. 主要入口

小程序底部 tab：

- 小区首页：进入业委专区的外层入口。
- 业委专区：主工作台，按角色展示会议、接待、学习、资料库、通知等入口。
- 个人中心：查看当前身份、通知入口和部分个人菜单。

主要页面：

- `pages/committee/committee`：业委会会议列表与新建会议。
- `pages/committee-detail/committee-detail`：会议详情，覆盖准备、进行中、结束后的不同视图。
- `pages/meeting-live-quick/meeting-live-quick`：快速会议，录音、识别、纪要生成相关流程。
- `pages/minutes/minutes`：会议纪要查看与编辑。
- `pages/minutes-public/minutes-public`：公开纪要查看。
- `pages/minutes-internal/minutes-internal`：内部 AI 议题报告。
- `pages/minutes-todos/minutes-todos`：会议待办事项。
- `pages/notifications/notifications`：通知中心。
- `pages/reception/reception`：接待登记与反馈。
- `pages/property-tasks/property-tasks`：物业侧任务处理。
- `pages/learning/learning`、`pages/learning-detail/learning-detail`：学习培训记录。
- `pages/library/library`、`pages/archive-detail/archive-detail`：资料库和归档详情。
- `pages/info-public/info-public`：公开信息。

## 5. 业委会会议流程

### 5.1 新建会议

主任/副主任进入会议管理页，点击右下角 `+` 新建业委会会议。

必填内容：

- 会议标题
- 会议日期
- 会议时间
- 会议地点
- 至少一个会议议题

可选内容：

- 会议资料预填
- 会议说明
- 议题类型，包括讨论、通报、表决等

当前真实后端行为：

- 新建会议会写入数据库。
- 会议默认进入 `preparing` 准备阶段。
- 后端会自动生成会议通知草稿 `noticeDraft`。
- 自动生成通知草稿不等于已经通知委员。

### 5.2 通知草稿与通知送达

新建会议后，会议详情页会展示通知草稿。主任/副主任可编辑会议要素或编辑通知正文。

当前语义：

- “生成通知草稿”：系统根据会议标题、时间、地点、说明自动生成通知正文。
- “发送通知”：选择通知对象后写入送达记录，委员通知中心才会出现对应通知。

也就是说，创建会议后不会自动把通知发送给委员，需要在准备阶段点击“选择通知对象并发送”。

### 5.3 准备阶段

准备阶段用于确认会前条件：

- 通知内容确认
- 通知对象选择和送达
- 材料送达
- 会议资料上传
- 会议基本信息维护
- 取消会议

当前已实现：

- 真实后端返回准备阶段详情。
- 即使旧数据缺少会议记录，准备阶段详情也会返回空记录对象，避免加载失败。
- 取消会议已是真实删除：仅准备阶段允许取消，后端会清理送达、记录、议题、投票、佐证、公示和纪要版本等关联数据。

### 5.4 开始会议

准备阶段满足条件后，主任/副主任可开始会议。

开始会议前置条件：

- 已设置会议日期。
- 已选择应参会成员并发送通知。
- 通知和材料已全部送达。
- 已有会议议题。

开始后会议进入 `ongoing` 阶段。当前默认会进入快速会议模式，支持录音、ASR、议题提取、纪要生成等真实后端接口。

### 5.5 进行中

进行中阶段主要处理：

- 委员确认参会
- 表决
- 代录
- 现场新增普通议题
- 上传佐证
- 快速会议录音、识别、纪要整理

快速会议相关接口强制走真实后端，即使 mock 开关打开也不会使用 mock。

### 5.6 结束会议与纪要

主任/副主任结束会议后，后端会根据出席、表决、佐证等信息判断会议状态：

- 有效
- 有效但带说明
- 无效

结束后可查看或编辑会议纪要。已公示或归档后的纪要修改会形成版本记录。

### 5.7 公示、撤回与归档

有效或带说明的会议可发起公示。公示后会记录公示状态、时间和操作人。

当前支持：

- 发起公示
- 撤回公示，需填写原因
- 查看公示状态
- 查看纪要版本历史
- 直接归档和撤销归档相关入口仍在页面中，后端支持情况需按具体接口继续验证

## 6. 通知中心

通知中心当前由真实后端接口提供：

- `GET /api/notifications`
- `PUT /api/notifications/{id}/read`
- `PUT /api/notifications/read-all`

当前实现方式：

- 通知由 `meeting_deliveries` 送达记录生成。
- 发送会议通知后，相关委员可在通知中心看到通知。
- 标记已读会回写送达记录中的已读时间。

## 7. 接待记录

接待记录用于登记业主来访和诉求处理。

主要能力：

- 查看接待制度
- 登记接待记录
- 分类诉求：物业类、公共事务、邻里纠纷、其他
- 填写处理结果
- 物业类事项可流转到物业任务页
- 业主侧和物业侧根据角色看到不同入口

## 8. 学习培训

学习培训用于记录业委会内部学习和街镇培训。

主要能力：

- 按内部学习、街镇培训、专项培训分类
- 新建学习记录
- 查看学习详情
- 上传佐证
- 推进学习阶段

## 9. 资料库与公开信息

资料库用于查看归档会议资料、纪要和补充材料。

公开信息用于展示已公示内容。未公示的内部会议流程和内部记录不应展示给普通业主或物业。

## 10. 当前真实后端与 mock 的关系

当前默认使用真实后端：

```js
var USE_MOCK = false;
```

`core.request(...)` 会访问真实后端；`core.realRequest(...)` 始终访问真实后端。

如果切回 `USE_MOCK = true`：

- 列表、新建、详情等普通接口会走本地 mock。
- mock 数据只存在于小程序运行内存中，重新编译或刷新后可能丢失。
- 快速会议、录音、ASR、纪要生成等仍然会走真实后端。

因此当前测试“新建会议是否持久化”“取消会议是否真实生效”时，应保持 `USE_MOCK = false`。

## 11. 常见问题

### 新建会议后列表不显示

检查：

1. `USE_MOCK` 是否为 `false`。
2. 后端是否启动在 `8080`。
3. 是否重新登录主任/副主任。
4. 当前列表是否停留在“准备”阶段。

### 通知中心 403

通常是未登录或本地 token 失效。清理微信开发者工具缓存并重新登录。

### 通知中心为空

创建会议只生成通知草稿，不会自动送达。需要进入准备阶段，点击“选择通知对象并发送”。

### 点通知进入旧会议 id 加载失败

旧 mock 数据或历史通知可能指向真实库不存在的会议 id。切到真实后端后，应重新登录并使用真实后端列表/通知。

### 准备阶段详情加载失败

已修复旧数据缺少会议记录导致的准备阶段详情失败。若仍失败，查看后端日志中具体接口和错误信息。

## 12. 当前已知注意事项

- 业主大会相关页面仍存在，但当前产品主线不建议作为演示重点。
- 前端文本和部分历史文档存在编码显示问题，运行时页面文件中较多中文已恢复，但仍需逐步清理。
- 会议材料上传当前部分接口仍需继续验证文件真实存储能力。
- 直接归档、撤销归档、公示后补充材料等链路已有入口，但建议在正式演示前逐项做接口回归。
- 后端使用 `ddl-auto: update`，新增字段会自动更新表结构；正式环境应改为 Flyway 迁移。
