# 老年人友好 · 社区业委会 App 设计规则

> 目标用户：业委会委员，多为中老年。以下规则全局适用于所有页面与组件。
> （2026-07-30 设计师定稿；由用户提供，记录于此作为全局规范。）

---

## 一、字号

| 用途 | 最小字号 |
| --- | --- |
| 正文 / 次要说明 | 14px |
| 列表标题 | 16.5px |
| 卡片主标题 | 18px |
| 页面主标题 | 22px |

不出现 12px 以下文字。

---

## 二、模块色（三色制）

| 模块 | 主色 | 页头副文字 |
| --- | --- | --- |
| 业委会会议 | `#2f5f9e`（蓝） | `#c5cede` |
| 业主接待 | `#2f6b45`（绿） | `#c6ddcf` |
| 学习培训 | `#2a6b73`（深青） | `#bdd8db` |

三色为同明度深色，页头压得住白字，主按钮也够稳。

### 只有三个地方可以用模块色

1. 页头背景
2. 底部导航选中项
3. 主按钮（实心）

### 禁止随模块变化

正文、次要灰字、卡片、分隔线、日期块、次级按钮底色 —— 三个模块**完全一致**。

### 跨模块恒定

异常态永远是暖橙（字 `#9a5b12` / 底 `#f7e4c6`），**不随模块变色**。
（注意：正因如此，学习培训不能用橙色做模块色，否则该页的警示色失效。）

---

## 三、基础配色

| 角色 | 色值 |
| --- | --- |
| 页面底 | `#f2f3f5` |
| 白卡 | `#ffffff` |
| 正文 | `#1f2937` |
| 次要灰（不得更浅） | `#6b7280` |
| 分隔线 | `#e2e5ea` |
| 页头文字 | `#ffffff` |

---

## 四、状态标签三级制

判断标准：**这个状态是否需要用户额外做点什么补救？**

| 级别 | 形式 | 用于 |
| --- | --- | --- |
| 1 | 暖色胶囊：`#9a5b12` 字 + `#f7e4c6` 底 | 异常态（逾期、待留档） |
| 2 | 模块色文字，无底色 | 今天需注意但不异常（今日、进行中） |
| 3 | 灰字 `#6b7280`，无底色 | 其余全部（待召开、待确认、计划中、已归档、待排期、已结束） |

- 常态一律不给底色。给了底色，异常就不显眼了。
- 一屏最多出现一种暖色胶囊，它是全页唯一跳出来的东西。

配套日期块：

- 异常项：`#fdf4e8` 底 + `#7a4a10` 字
- 其余：`#f4f6f9` 底 + `#4b5563` 字

---

## 五、层级：白卡 = 要办的事

- **白卡 + 轻阴影**（`0 1px 3px rgba(31,41,55,.07)`）：需要用户处理的事项。
- **透明底 + 分隔线的轻列表**：只需知晓的信息（未来排期、历史记录）。
- 两者间距 20px 左右，不要更大，避免看起来断开。
- 不要卡中卡（白卡里再套白卡）。

---

## 六、点击目标

- 主要按钮高度 ≥ 54px；图标按钮 ≥ 54×54；底部导航每格 ≥ 44px。
- 列表行点击 = 进入详情（通用心智）。不要用“点击 = 切换选中”这种点了不进去的交互。

---

## 七、主操作

- 主操作固定在屏幕底部拇指区，紧贴底部导航上沿（中间留 8–10px 间隙 + 分隔线），不随内容长度漂移，不放在页面中部。
- 底部操作条最多一主一次：主按钮实心，次按钮浅底；上下叠放或按 2:1 分宽。
- 按钮文案带上操作对象（如「第3次例会 · 应于 5-6月 / 去补开」），不靠位置让用户推断对应关系。
- 低频动作（一年数次）不占底部，收进列表末行做「＋ xxx ›」。

---

## 八、文案

- 只写状态事实，不写制度解释、使用说明、重复信息。
- 界面结构能表达的，不要再用文字说一遍（如“底部按钮指向此项”这类提示一律不写）。
- 一屏内避免连续三行以上说明文字。
- 不同状态下的文案必须自洽：空态、逾期态、进行中态之间不能互相矛盾。
- 未确定日期的事项写时间窗（如“预计 9–10月”），不要编造具体日期。

---

## 九、折叠与截断

- 常看信息不折叠 —— 默认收起会让用户以为没有内容。
- 条目变多时用**截断**而非折叠：显示前 2 条 + 一行「还有 N 场 ›」进入完整列表。
- 折叠只用于低频且长的内容（如“全年会议一览”），且不要为填补空白而默认展开 —— 空白比塞满好。

---

## 十、危险操作

列表行上不放删除按钮（×）。删除移入详情页，并带二次确认。

---

## 十一、状态归属

- 已发生的记录（如「接待记录」）只有一种状态，不写状态标签；唯一例外是“应做未做”（待留档），按异常处理。
- 待跟进 / 进行中 / 已完成属于**事项**（业主反馈转化的待办），不属于记录本身。两者不要混在同一个列表里。
