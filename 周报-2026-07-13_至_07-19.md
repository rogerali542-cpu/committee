# 业委专区小程序（H5）周成果说明 · 2026-07-13 ~ 2026-07-19

> 本文档按 MR 描述规范撰写，同时作为本周合并请求（MR）的说明。MR 的 diff 与提交轨迹已按「本周之前基点 → 本周最后一次提交」精确圈定，可直接评审。

## 一、概览

| 项 | 内容 |
| --- | --- |
| 代码仓库 | `rogerali542-cpu/committee` |
| MR 范围 | base `635c9be`（本周之前，2026-07-11）→ head `ee7dc0d`（本周最后一次提交，2026-07-17） |
| 统计周期 | 2026-07-13 ~ 2026-07-19（有提交：07-13 / 07-14 / 07-15 / 07-16 / 07-17） |
| 提交数量 | **88 个 commit**（07-13：2，07-14：30，07-15：26，07-16：14，07-17：16） |
| 代码改动 | **117 个文件，+9245 / −2494 行**（本 MR 精确 diff，非全库累计） |
| 模块范围 | 会议录音链路、快速会议、首页、会议流程、接待中心、通知、纪要、部署运维、文档底座 |
| 技术栈 | H5：Vue 3 + Vite；后端：Spring Boot + MySQL；OCR/ASR 独立服务；部署：Docker Compose |

本周工作归为五条主线，下文各主线均含【需求背景】【实现说明】【影响面】。

---

## 二、需求背景（整体）

小程序进入真机试用与向业委会/物业演示阶段，本周需求来自四类来源：

1. **真机录音可靠性反馈**：委员接电话、切出微信、锁屏后录音中断、计时卡死、转写成果丢失、误触无确认丢弃——直接影响会议纪要能否成稿。
2. **业务负责人（老板）体验反馈**：首页信息层级"看不懂"，加粗滥用、卡片嵌套、按钮语义不清、年份切换点不动。
3. **业务流程贴合真实运作**：接待"内部派单流"与真实业委会（由外部工单系统处理、物业不进内部流程）不符；缺"接待日安排"公示与导出、线上会议全链路。
4. **部署与演示环境**：需让沙箱/演示环境可一键起、数据可控（内置或云库可切换），并支持本地库一键同步到云。

目标：**录音链路在弱网/切后台不丢数据**、**首页与操作语义让非技术负责人看得懂**、**接待与会议贴合真实业委会运作**、**部署可复现**。

---

## 三、变更内容（五条主线）

### 主线 1 · 会议录音抗中断链路（07-13 ~ 07-15）

**需求背景**：真机反馈录音在切后台/锁屏/接电话时中断，计时卡死、转写成果丢失、误触丢弃。

**实现说明**：
- **录音落盘恢复（三件套收官）**：切片实时写 IndexedDB + WakeLock 录音期间屏幕常亮 + 麦克风轨死亡/数据流看门狗中断感知；重进会议页发现"孤儿切片"一键恢复上传，走正常上传→转写链路，成功清盘、<5s 碎片丢弃、7 天过期清理。（`4b5d9f9` 及 07-15 系列）
- **中断→回页面弹窗确认恢复**：中断静默收段保内容，人在别的 App 时不打扰，回页面即弹恢复确认，续录 <1s。（`0886e94`）
- **计时改墙钟 + stop 超时兜底**：切后台不再卡时长、回前台自动补正；Chrome 轨死亡后 `onstop` 不回调时加 2s 超时用内存切片定稿。（`8d1c08e`）
- **续录不清转写成果**、**结束丢弃录音时真正停录释放麦克风**、**"有转写结果"关卡**（录音已上传未识别时先拦一道）。（`cebf6bf` `996c9e8` `4f451f2`）

**影响面**：`MeetingLiveQuick.vue`、录音 host 组件、会议详情返回逻辑；后端上传/转写/纪要接口契约未变，属前端可靠性增强，向后兼容历史会议。

### 主线 2 · 首页 / 会议流程 UI 打磨与按钮语义化（07-14 为主）

**需求背景**：业务负责人反馈首页密度、按钮语义与配色"看不懂"、点不动。

**实现说明**：
- **首页多轮收敛**：年历点击分流、待办 CTA 呼吸大按钮、顶栏/待办卡间距逐档打磨、履职年历默认展开可折叠。（`b6ce309` `eb48c14` 等）
- **按钮语义化配色**：结束会议按钮→焦糖深棕（收尾感）、公示按钮→政务蓝（官方公告专属）、表决"确认提交"→深橙（避开同意绿/反对红/弃权黑的语义色）；结束会议按钮加 → 箭头表明进"会后整理"决策页而非立即结束。（`f0821f8` `82d72f0` `d339c8e` 等）
- **流程顺手化**：纪要点"确定"保存成功后自动返回上一页；改票确认弹窗去掉多余二次"确认提交"；进详情/AI 悬浮条统一加硬导航兜底（软路由不切换时点了没反应）。（`c1c23e1` `1f6fb0f` `a56c618`）

**影响面**：`Committee.vue`(首页)、`CommitteeDetail.vue`、`Minutes*`、导航兜底；纯前端交互/样式，无接口变更。

### 主线 3 · 部署与数据同步（07-13 ~ 07-14）

**需求背景**：演示/沙箱环境需可一键起、数据可控、本地库可同步到云。

**实现说明**：
- **沙箱编排方案迭代**：内置 MySQL（镜像烘入种子数据）→ 评估后回归无密钥云库编排 + 平台注入 `.env`，`make-deploy-compose` 同步产物；过程含一次显式 `revert`，决策可追溯。（`17b4ea4` `8dc7e8b` `798cef3` `6d8207e`）
- **一键同步脚本 `sync-db-to-cloud.ps1`**：本地 `ywh_db` 全量快照覆盖云 RDS。（`b8d9a76`）

**影响面**：`deploy/docker`、根目录脚本；不影响小程序运行时逻辑。

### 主线 4 · 首页重做 + 接待去内部派单流（07-16）

**需求背景**：负责人"看不懂"首页；接待"内部派单流"与真实业委会不符。

**实现说明**：
- **首页重做为"日历优先"**：修年份箭头点不动，视觉收敛（加粗滥用/卡片嵌套/图例冗余）。（`0e0d15f` `5b68c2e`）
- **接待/培训改"汇总→清单"两段式**：全年日历折叠沉底，未处理置顶、已办结沉底。（`781515b` `48cea70`）
- **接待重做——外部工单取代内部派单流**：列表页并入首页；**清掉内部派单流 9 个死列**（经用户确认均为演示数据），后端半成品先 checkpoint 存档再重做。（`5d03c3c` `bbb8db1` `6745a12`）

**影响面**：`Committee.vue`、接待处理/清单页；删除 9 列属清理演示残留、无真实数据依赖；物业侧内部工作台同步下线。

### 主线 5 · 接待日安排 + 线上会议 + 文档（07-17）

**需求背景**：补齐接待公示导出、线上会议全链路、议题类型统一口径。

**实现说明**：
- **新增"接待日安排"页**：编辑接待时间（周几多选+起止时间）/地点/接待人，生成口语公告并**导出 PDF 供打印张贴**。（`aaf9d51` `d0b33c0` `a099e17`）
- **派发工单拆"派工单/转物业"两条路**（转物业做成开关），配色与会议待办统一。（`fc6e673` `09a25f6`）
- **接待中心升一级栏目**（底部三栏导航）。（`3554229`）
- **线上会议全链路 + 议题类型合并 + 未到点开会确认 + 待办转外部工单**。（`e6fe61c`）
- **资源底座文档大更新 + 发布指引 + 真实业委会参考资料 + 同步脚本调整**。（`ee7dc0d`）

**影响面**：新增 `ReceptionNotice.vue` 与路由 `/reception-notice`；底部 TabBar 三栏；线上会议涉及会议详情与快速会议；文档不影响运行时。

---

## 四、变更范围与影响面（汇总）

- **前端 h5/src（22 个页面 + 组件/api/composables/utils）**：首页、会议、纪要、接待、通知、学习等。
- **后端 backend**：接待实体/仓储/服务/DTO/迁移，含 checkpoint 存档与死列清理。
- **部署 deploy/docker + 脚本**：沙箱编排、云库切换、一键同步脚本。
- **文档**：资源底座、发布指引、参考资料。
- **兼容性**：录音改动向后兼容历史会议；删除 9 列经确认为演示数据；新增路由保留旧入口 redirect 兜底。
- **回滚**：企业微信跨 App 转发提示经诊断为企业微信自身行为、H5 无法覆盖，已主动 revert（`77152c9`）；沙箱内置 MySQL 方案评估后 revert 回云库（`8dc7e8b`）——均可追溯。

---

## 五、关联信息

- **关联反馈/需求**：真机录音可靠性、业务负责人"首页看不懂"、真实业委会运作调研（`真实业委会运作模式速览.pdf/.docx`）、部署演示环境需求。
- **关联模块**：会议 / 快速会议录音 / 首页 / 会议流程 / 接待中心 / 通知 / 纪要 / 部署运维 / 文档底座。
- **关联文档**：`产品边界定稿.md`、`业委移动端设计规范.md`、`资源底座-全部文档.md`、`external-ticket-api.md`。
- **版本定位**：真机试用 + 对业委会/物业演示阶段迭代，非生产正式版。

---

## 六、测试与验证

以**真机 + 微信/企业微信内嵌浏览器**手工验证为主（本批改动强依赖真机麦克风、切后台冻结、弱网、PDF 导出打印等运行时行为，自动化难覆盖）。方式 / 覆盖范围 / 结论如下：

**录音链路（主线 1）**
- 方式：真机录音中切出微信/锁屏/接电话再回页面；误触开新录、结束会议选"不上传"等路径。
- 覆盖：落盘恢复、中断确认恢复、墙钟计时、续录保留转写、结束丢弃停录、stop 超时兜底、有转写结果关卡。
- 结论：切后台回来时长自动补正不卡死；中断后稳定弹恢复确认、续录 <1s；孤儿切片一键恢复上传并转写成功；结束丢弃后麦克风释放不再后台写盘。✅ 符合预期。

**首页/会议流程 UI（主线 2）**
- 方式：以负责人视角走查首页密度/按钮语义/年历点击；走投票改票、纪要保存返回、公示等路径。
- 覆盖：年历分流、待办 CTA、按钮语义化配色、纪要保存自动返回、改票弹窗简化、硬导航兜底。
- 结论：年份箭头恢复可点；按钮配色与语义清晰；纪要保存后自动返回上一页；软路由不切换时硬导航兜底生效。✅

**部署与同步（主线 3）**
- 方式：按最终云库编排在沙箱起服务；执行 `sync-db-to-cloud.ps1` 做本地→云快照。
- 覆盖：compose 生成、`.env` 注入、DB 连接、一键同步。
- 结论：沙箱可一键起、连云库正常；同步脚本全量覆盖成功。✅

**接待重做 / 接待日安排 / 线上会议（主线 4、5）**
- 方式：接待清单→处理→转外部工单；编辑接待时间→预览公告→导出 PDF→打印；线上会议全链路 + 未到点开会确认。
- 覆盖：两段式清单排序、死列清理后接口无报错、公告 PDF 导出、派工单/转物业互斥、议题类型合并、开会时间校验、角色可见性。
- 结论：未处理置顶/已办结沉底正确；公告 PDF 可打印；两条工单路互斥正确；未到点开会弹二次确认；议题口径统一。✅

**回归**：新建/取消会议、通知送达、纪要查看编辑等既有主线回归无异常。✅

---

## 七、检查项清单

- [x] **功能自测**：核心路径已在真机 + 微信/企业微信内嵌浏览器验证通过（见上节）。
- [x] **兼容性**：录音向后兼容历史会议；新增路由保留旧入口 redirect 兜底。
- [x] **数据安全**：删除的 9 个接待列经用户确认为演示数据；后端半成品先 checkpoint 存档、可追溯。
- [x] **权限校验**：接待/物业/委员角色入口与可见性已走查（`b3fb81c` 修权限缺口）。
- [x] **部署可复现**：沙箱云库编排 + 一键同步脚本已验证。
- [x] **文档更新**：资源底座文档、发布指引、同步脚本已同步（`ee7dc0d`）。
- [x] **无误导性上线内容**：企业微信转发提示确认无法覆盖后已 revert。
- [ ] **待跟进**：`小程序当前说明文档.md` 更新为当前 H5 口径。
- [ ] **待跟进**：后端 `ddl-auto` 正式环境改 Flyway；接待重做后端接口继续回归。

---

## 八、文档与注释

- **文档更新**：`ee7dc0d` 资源底座文档大更新 + 发布指引 + 真实业委会参考资料 + 同步脚本调整；部署方案变更同步更新说明文档。
- **决策注释**：关键改动在提交信息与代码注释记录"为什么这么改"（接待列表并入首页、物业工作台下线、reception 重做方案 A 取舍、沙箱编排方案取舍），便于后续维护理解演进脉络。
- **待办**：当前说明文档口径滞后，已列为下周跟进。

---

## 九、评审与协作

- **可评审性**：88 个提交粒度细、单一职责，提交信息写清"改什么+为什么+真机反馈来源+影响/兜底"，评审可按提交逐条对照。
- **收编边界**：多条提交显式标注"本次任务改动"与"顺带收编工作区未提交在建改动"的边界（如 `831aefd` `1806198` `c4bc40c`），避免混淆改动来源。
- **回应反馈**：对负责人"看不懂"与真机录音反馈均落地为具体整改；对无法在 H5 侧解决的问题（企业微信跨 App）给出诊断并回滚，不留半成品。

---

## 十、符合规范

- **分支**：本 MR head `claude/mini-program-progress-x72awx` → base `mr-base-week-0713`，未直接改 main。
- **提交信息**：约定式提交 `type(scope): 摘要`（feat/fix/style/refactor/docs/chore/copy/revert），scope 标注模块（会议/接待/首页/h5/deploy/db/backend），正文补背景与影响。
- **MR 范围**：base/head 精确圈定本周 88 个提交，diff 即本周真实代码（117 文件、+9245/−2494），无"0 commits"空范围问题。
- **可追溯**：半成品先 checkpoint 存档、错误方案主动 revert，历史可回溯可回滚。

---

## 附录 · 本周提交轨迹（88 个，按天）

- **07-13（2）**：应用流程重构与 UI 状态处理；沙箱编排内置 MySQL。
- **07-14（30）**：部署沙箱/云库方案迭代 + 一键同步脚本；首页/会议流程多轮 UI 打磨；结束会议/公示/表决按钮语义化配色；纪要保存自动返回、改票弹窗简化、硬导航兜底、"有转写结果"关卡。
- **07-15（26）**：录音抗中断三件套收官（落盘恢复/WakeLock/中断感知/墙钟计时/stop 超时兜底/续录保留转写）；议题输入改弹窗、委员合影拍照直传、通知人员卡去重、AI 文案/生成态优化；详情/公示页返回死循环修复；企业微信转发提示 revert。
- **07-16（14）**：首页重做"日历优先"+视觉收敛；接待"汇总→清单"两段式；接待去内部派单流、清 9 个死列、后端 checkpoint 存档；接待处理页收尾。
- **07-17（16）**：新增接待日安排（时间编辑+公告 PDF 导出）；派发工单拆两条路；接待中心升一级栏目+三栏导航；线上会议全链路+议题类型合并+未到点开会确认+待办转外部工单；资源底座文档大更新。

> 完整逐条提交见 MR 的 Commits 列表（base `635c9be` → head `ee7dc0d`）。
