# 敏感信息保护



### 数据最小化原则

- 只收集必要数据：收集个人信息应限于实现处理目的的最小范围，不过度收集。

- 优先设备端处理：能在用户设备上本地处理的数据，就不要上传到服务器。这能极大降低数据在传输和存储过程中的泄露风险。



### 数据加密与脱敏


- 存储与传输加密：密码，密钥等认证信息存储的时候需要加密， 支付信息等敏感信息在网络上传输的时候需要使用HTTPS协议进行加密

  对于密码加密需要分场景处理：

  1. 比如做用户登录认证的密码，在只需要做认证的场景下是不需要解密为原始明文的，所以保存的时候使用HASH+SALT保存即可
  2. 比如调用第三方系统的API Key，使用的时候必须要明文，一般使用AES-256-GCM算法进行加密保存，使用的时候解密到内存中使用即可

- 静态信息脱敏：

  个人的手机号或者身份证信息保存到数据库的时候需要加密保存，不能明文保存到数据库中，需要在前端显示用户的手机号或者身份证号等敏感信息的时候需要对手机号或者身份证号的某些位进行掩码显示，比如：138****0000



### 敏感信息访问审计

- 用户需要访问明文的敏感信息的时候，后台需要记录相关的审计日志。
- 敏感信息在记录 日志的时候一定要 脱敏记录，不能记录明文



### 加密密钥保护

- 加密密钥禁止硬编码到代码里面。

- 本地如果需要保存加密密钥，可以参考下面的保存方案：

  可以把密钥分两部分，一部分放到环境变量中，一部分放到key文件中（文件的权限为600），启动docker容器的时候从两边 读取拼接成完整加密密钥使用。

  

  

  

